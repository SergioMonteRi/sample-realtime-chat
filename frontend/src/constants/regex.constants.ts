/** Conteudo composto apenas de espacos, quebras de linha ou tabulacoes. */
export const ONLY_WHITESPACE_REGEX = /^\s*$/

/** Tudo que vem antes do "@" em um e-mail. */
export const EMAIL_LOCAL_PART_REGEX = /^([^@]+)@/
